import React from "react";
import Logo from "../Logo/Logo";

const FooterTitle = () => {
  return (
    <div>
      <Logo></Logo>
      <p>Donate blood to save human lives.</p>
    </div>
  );
};

export default FooterTitle;
